[project]
name = "dalgubeol-now"
version = "4.1.0"
description = "Daily Dalgubeol-daero traffic dashboard using a local ITS collector, Vercel FastAPI, and Neon Postgres"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.141.1",
  "psycopg[binary]>=3.2.0"
]
